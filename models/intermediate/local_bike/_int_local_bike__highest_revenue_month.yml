version: 2

models:
  - name: _int_local_bike__highest_revenue_month
    description: '{{ doc("_int_local_bike__highest_revenue_month") }}'
    columns:
      - name: order_date
        description: "Primary key, unique identifier for each total revenue."
        tests:
          - unique
          - not_null
